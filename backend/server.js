import 'dotenv/config';import http from 'http';import {Server} from 'socket.io';import {connectDatabase} from './config/db.js';import {createApp} from './app.js';
if(!process.env.JWT_ACCESS_SECRET||!process.env.JWT_REFRESH_SECRET)throw new Error('JWT_ACCESS_SECRET and JWT_REFRESH_SECRET are required');
const app=createApp();const server=http.createServer(app);const origins=(process.env.CLIENT_URL||'http://localhost:5173').split(',');const io=new Server(server,{cors:{origin:origins,credentials:true}});app.set('io',io);io.on('connection',socket=>socket.emit('connected',{message:'Welcome to Darna'}));connectDatabase().then(()=>server.listen(process.env.PORT||5050,()=>console.log(`API listening on ${process.env.PORT||5050}`))).catch(err=>{console.error('Database connection failed',err);process.exit(1)});
